<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->a($this->url->get("categories/index"), "Go Back"); ?></li>
            <li class="next"><?php echo $this->tag->a($this->url->get("categories/new"), "Create"); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID Of CATEGORIE</th>
            <th>NOM Of CATEGORIE</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->getItems() as $categorie): ?>
            <tr>
                <td><?php echo $categorie->ID_CATEGORIE; ?></td>
            <td><?php echo $categorie->NOM_CATEGORIE; ?></td>

                <td><?php echo $this->tag->a($this->url->get("categories/edit/" . $categorie->ID_CATEGORIE), "Edit"); ?></td>
                <td><?php echo $this->tag->a($this->url->get("categories/delete/" . $categorie->ID_CATEGORIE), "Delete"); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->getCurrent(), "/", $page->getTotalItems() ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->a($this->url->get("categories/search"), "First", ['class' => 'page-link', 'id' => 'first']) ?></li>
                <li><?php echo $this->tag->a($this->url->get("categories/search?page=" . $page->getPrevious()), "Previous", ['class' => 'page-link', 'id' => 'previous']) ?></li>
                <li><?php echo $this->tag->a($this->url->get("categories/search?page=" . $page->getNext()), "Next", ['class' => 'page-link', 'id' => 'next']) ?></li>
                <li><?php echo $this->tag->a($this->url->get("categories/search?page=" . $page->getLast()), "Last", ['class' => 'page-link', 'id' => 'last']) ?></li>
            </ul>
        </nav>
    </div>
</div>
