<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->a($this->url->get("produits/index"), "Go Back"); ?></li>
            <li class="next"><?php echo $this->tag->a($this->url->get("produits/new"), "Create"); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>ID Of PRODUIT</th>
            <th>ID Of CATEGORIE</th>
            <th>NOM Of PRODUIT</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->getItems() as $produit): ?>
            <tr>
                <td><?php echo $produit->ID_PRODUIT; ?></td>
            <td><?php echo $produit->ID_CATEGORIE; ?></td>
            <td><?php echo $produit->NOM_PRODUIT; ?></td>

                <td><?php echo $this->tag->a($this->url->get("produits/edit/" . $produit->ID_PRODUIT), "Edit"); ?></td>
                <td><?php echo $this->tag->a($this->url->get("produits/delete/" . $produit->ID_PRODUIT), "Delete"); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->getCurrent(), "/", $page->getTotalItems() ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->a($this->url->get("produits/search"), "First", ['class' => 'page-link', 'id' => 'first']) ?></li>
                <li><?php echo $this->tag->a($this->url->get("produits/search?page=" . $page->getPrevious()), "Previous", ['class' => 'page-link', 'id' => 'previous']) ?></li>
                <li><?php echo $this->tag->a($this->url->get("produits/search?page=" . $page->getNext()), "Next", ['class' => 'page-link', 'id' => 'next']) ?></li>
                <li><?php echo $this->tag->a($this->url->get("produits/search?page=" . $page->getLast()), "Last", ['class' => 'page-link', 'id' => 'last']) ?></li>
            </ul>
        </nav>
    </div>
</div>
